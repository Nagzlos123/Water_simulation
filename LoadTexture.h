#ifndef TEXTURE_LOADER_HPP
#define TEXTURE_LOADER_HPP 1
/*
* #include <iostream>
#include <stdexcept>
#include <GL/glew.h>
#include <GL/glext.h>

using namespace std;
class LoadTexture
{
	static GLenum getFormat(const unsigned& channelsNumber);

public:
	static void loadTexture(unsigned& textureIdex, const string& fileName);
};
*/

#endif
